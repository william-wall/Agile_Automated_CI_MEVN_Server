dist: trusty
language: node_js
node_js:
- stable
script:
- npm run start-coverage
after_success:
- npm run publish-coverage
branches:
    only: development_ci
notifications:
     email:
        recipients:
        - williamwalldeveloper@gmail.com
        on_success: never
        on_failure: always